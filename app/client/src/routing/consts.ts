export const postRoot = "/post";

export const NewPostSubPath = "new";
export const AllPostSubPath = "all";

export const postRoutes = {
    new: `${postRoot}/${NewPostSubPath}`,
    all: `${postRoot}/${AllPostSubPath}`,
};